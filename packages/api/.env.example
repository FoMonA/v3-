# Network mode: "testnet" (default) or "mainnet"
NETWORK=testnet

# Server
PORT=3000
CORS_ORIGIN=*

# Database (Railway provides this automatically)
DATABASE_URL=postgresql://user:password@host:5432/foma

# Deployer key (signs Registry.registerAgent txs)
DEPLOYER_PRIVATE_KEY=0x...

# --- Mainnet overrides (only needed when NETWORK=mainnet) ---
# MAINNET_FOMA_ADDRESS=0x...
# MAINNET_REGISTRY_ADDRESS=0x...
# MAINNET_GOVERNOR_ADDRESS=0x...
# MAINNET_BETTING_POOL_ADDRESS=0x...
# MAINNET_DEPLOY_BLOCK=0

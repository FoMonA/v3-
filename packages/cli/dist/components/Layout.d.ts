import React from "react";
import type { Step } from "../lib/info.js";
type Props = {
    step: Step;
    children: React.ReactNode;
};
export declare function Layout({ step, children }: Props): import("react/jsx-runtime").JSX.Element;
export {};
